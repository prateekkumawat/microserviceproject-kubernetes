
# create secret for ecr repo login 

kubectl create secret docker-registry  ecr-login --docker-username=AWS \
 --docker-server={awsaccountid}.dkr.ecr.ap-south-2.amazonaws.com \
 --docker-password=$(aws ecr get-login-password) 